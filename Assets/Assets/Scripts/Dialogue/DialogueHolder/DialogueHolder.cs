using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DialogueHolder : MonoBehaviour
{
    [Header("Dialogue tree to initiate")]
    [Tooltip("this script only works mostly as a script holder")]
    public DialogueScriptableObject dialogueToInitiate;
}
